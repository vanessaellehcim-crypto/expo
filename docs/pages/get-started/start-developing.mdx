
src/screens/LedgerScreen.js

import React, {useState, useEffect} from 'react';
import { View, Text, FlatList, Button, StyleSheet } from 'react-native';
import axios from 'axios';

const mockEntries = [
  { id: '1', title: 'Guardian Seal Active', desc: 'Omni-Eternal pulse anchored', date: '2025-09-01' },
  { id: '2', title: 'Positive Reinforcement Loop', desc: 'Rachel loop anchored', date: '2025-09-01' },
];

export default function LedgerScreen({ navigation }) {
  const [entries, setEntries] = useState(mockEntries);

  // Placeholder: later hook to load from secure API (your Crown Vault)
  useEffect(() => {
    // fetch ledger entries if you add a backend
  }, []);

  return (
    <View style={styles.container}>
      <Text style={styles.header}>Royal Ledger</Text>
      <FlatList
        data={entries}
        keyExtractor={(i) => i.id}
        renderItem={({item}) => (
          <View style={styles.item}>
            <Text style={styles.itemTitle}>{item.title}</Text>
            <Text style={styles.itemDesc}>{item.desc}</Text>
            <Text style={styles.itemDate}>{item.date}</Text>
          </View>
        )}
      />
      <View style={{height:10}}/>
      <Button title="Back" onPress={() => navigation.goBack()} />
    </View>
  );
}

const styles = StyleSheet.create({
  container:{ flex:1, padding:16, backgroundColor:'#071023' },
  header:{ color:'#fbbf24', fontSize:20, fontWeight:'700', marginBottom:12 },
  item:{ backgroundColor:'#0b1220', padding:12, borderRadius:8, marginBottom:10 },
  itemTitle:{ color:'#fff', fontSize:16, fontWeight:'600' },
  itemDesc:{ color:'#cbd5e1', marginTop:4 },
  itemDate:{ color:'#94a3b8', marginTop:6, fontSize:12 }
});

src/screens/VaultScreen.js

import React from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

export default function VaultScreen({ navigation }) {
  // Placeholder - show Crown Vault balance (static until you connect real APIs)
  const fakeBalance = '0.00 PHILALUX';

  return (
    <View style={styles.container}>
      <Text style={styles.header}>Crown Vault</Text>
      <Text style={styles.balance}>{fakeBalance}</Text>
      <Text style={styles.note}>All revenues flow to Crown Vault (placeholder)</Text>
      <View style={{height:10}}/>
      <Button title="Back" onPress={() => navigation.goBack()} />
    </View>
  );
}

const styles = StyleSheet.create({
  container:{ flex:1, padding:16, alignItems:'center', justifyContent:'center', backgroundColor:'#021027' },
  header:{ color:'#fbbf24', fontSize:20, fontWeight:'700' },
  balance:{ color:'#e6edf3', fontSize:18, marginTop:12 },
  note:{ color:'#94a3b8', marginTop:8, textAlign:'center' }
});

app.json (minimal Expo config — change slug/name)

{
  "expo": {
    "name": "Crown Vault Ledger",
    "slug": "crown-vault-ledger",
    "version": "1.0.0",
    "android": {
      "package": "com.crown.michelle.crownvault",
      "versionCode": 1,
      "permissions": []
    }
  }
}
